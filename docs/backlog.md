# Backlog

Items that are deferred, blocked on external dependencies, or otherwise not part of the current sprint plan.

## Needs Browser Extension

- **Focus Blocker** — UI removed 2026-04-25. Re-enable when companion browser extension exists. Schema column `pomodoro_settings.blocked_sites` preserved for forward compatibility.
