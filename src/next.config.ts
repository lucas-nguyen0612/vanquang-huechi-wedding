import type { NextConfig } from "next";

// Font optimization is handled automatically by next/font (Inter + JetBrains Mono)
// See app/layout.tsx for font configuration
const nextConfig: NextConfig = {};

export default nextConfig;
